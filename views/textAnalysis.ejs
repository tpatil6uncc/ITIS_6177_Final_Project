<%- include('./partials/header.ejs') %>
<main class="main_content">
    <div class="container">

<% if(lines) { %>

    <h2 mb-3>Image contains below lines</h1>

    <% lines.forEach(line=>{ %>

        <h4><%=line %></h2>

        <%}) %>

<%} %>

    </div>
    </main>

<%- include('./partials/footer.ejs') %>